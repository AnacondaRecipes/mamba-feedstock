anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: mamba-org/mamba
      use_max_tag: true
      include_regex: ^(?!20\d{2}\.)[\d.]+$
    packages:
      - libmamba
      - libmambapy
